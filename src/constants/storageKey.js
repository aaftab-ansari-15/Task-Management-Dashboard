const STORAGE_KEYS = {
    USERS: "Users", // Key for storing all users' data in localStorage
    CURRENT_USER: "CurrentUser", // Key for storing the currently logged-in user in sessionStorage
    ALL_USERS_TASKS: "AllUsersTasks", // Key for storing all tasks in localStorage
    CURRENT_USER_TASKS: "CurrentUserTasks", // Key for storing current user's tasks in sessionStorage
  };
  export default STORAGE_KEYS;